# Environment: development | staging | production
NODE_ENV=development

PORT=3000

DATABASE_URL=postgresql://user:password@localhost:5432/database?schema=public
DIRECT_URL=postgresql://user:password@localhost:5432/database?schema=public

ACCESS_TOKEN_DURATION=15m
ACCESS_TOKEN_SECRET=your-access-token-secret

REFRESH_TOKEN_DURATION=7d
REFRESH_TOKEN_SECRET=your-refresh-token-secret

# GCS Storage Configuration (required for staging/production, not needed for development)
# In development, files are stored locally in the media/ folder
# Uses Application Default Credentials (ADC) - no explicit service account credentials needed
# In Cloud Run, ADC automatically uses the Cloud Run service account identity
GCS_BUCKET_NAME=your-bucket-name

# Scheduler token for cronjob 
SCHEDULER_TOKEN=your-scheduler-token